#:import random random


<SnakePart>:
    size: 80, 80
    canvas.before:
        Color:
            rgb: random.random(),random.random(),random.random()
        Rectangle:
            size: self.size
            pos: self.pos


GameScreen:
    canvas.before:
        Color:
            #rgba: 0.4, 0.3, 0.8, 1          #cervena
            rgba: 0.29803, 0.4, 0.8, 1          #modra

            #rgb: 1,1,1
        Rectangle:
            size: self.size
            pos: self.pos
    Widget:
        size: 60, 60
        pos: 120, 120
        id: food
        canvas.before:
            Color:
                rgb: 1,1,0
                #rgb: random.random(),random.random(),random.random()
            Ellipse:
                size: self.size
                pos: self.pos

